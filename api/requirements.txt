# Core ML dependencies (pinned to match pyannote-audio 4.0.x requirements;
# torchcodec releases are tightly coupled to torch minor versions — bump together)
torch==2.8.0
torchaudio==2.8.0
torchcodec==0.7.0

huggingface-hub==1.2.1

# Pyannote speaker diarization (4.0.x required for community-1 model with PLDA)
pyannote.audio==4.0.7

# FastAPI and server
fastapi==0.124.0
uvicorn[standard]==0.38.0
python-multipart==0.0.20

# Qdrant vector database client (keep within 1 minor of the server image
# pinned in docker-compose.yml)
qdrant-client==1.18.0

# Pydantic for settings and models
pydantic==2.12.5
pydantic-settings==2.12.0

# Audio processing
scipy==1.16.3
numpy==2.3.5

# HTTP client for Whisper API
httpx==0.28.1

# Utilities
python-dotenv==1.2.1
